/*
 * Make a program to detect if the inputted string is a palindrome
 * Use get_string() to get the string from the user
 */

#include<cs50.h>
#include<stdio.h>
#include<string.h>

int main(void)
{
    // Starting code - get the string from the user
    string input = get_string("What would you like to check");

    // Todo 1: Get the length of the input

    // Todo 2: Check if it's a palindrome.
        // Start by comparing the first and last chars
        // Then the 2nd and 2nd-to-last
        // Etc...

}